# 🏗️ Architecture Visual Guide

## System Overview Diagram

```
┌─────────────────────────────────────────────────────────────────────────┐
│                              INTERNET / USERS                            │
│                                                                           │
│  👨‍💼 Admins    👨‍🏫 Teachers    👨‍🎓 Students    📱 Mobile Users          │
└─────────────────┬─────────────────────────────────────────────────────┘
                  │
                  │ HTTP/HTTPS
                  ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                     NGINX REVERSE PROXY (Port 80/443)                    │
│                         API Gateway & Load Balancer                       │
│                                                                           │
│  Routes:                                                                  │
│  • /admin/     → Admin Frontend                                          │
│  • /teacher/   → Teacher Frontend                                        │
│  • /student/   → Student Frontend                                        │
│  • /api/       → Backend APIs                                            │
│  • /static/    → Static Files                                            │
└─────────────────┬─────────────────────────────────────────────────────┘
                  │
      ┌───────────┼───────────┐
      │           │           │
      ▼           ▼           ▼
┌──────────┐ ┌──────────┐ ┌──────────┐
│ Admin    │ │ Teacher  │ │ Student  │
│ Frontend │ │ Frontend │ │ Frontend │
│          │ │          │ │          │
│ Next.js  │ │ Next.js  │ │ Next.js  │
│ Port:    │ │ Port:    │ │ Port:    │
│ 3000     │ │ 3001     │ │ 3002     │
└────┬─────┘ └────┬─────┘ └────┬─────┘
     │            │            │
     │            │            │
     └────────────┼────────────┘
                  │
                  │ HTTP Requests
                  ▼
      ┌───────────────────────┐
      │                       │
      ▼                       ▼
┌─────────────┐      ┌─────────────┐
│   Django    │      │  FastAPI    │
│   Backend   │◄────►│  Backend    │
│             │      │             │
│ • ORM       │      │ • Files     │
│ • Auth      │      │ • WebSocket │
│ • Admin     │      │ • Tasks     │
│ Port: 8001  │      │ Port: 8000  │
└──────┬──────┘      └──────┬──────┘
       │                    │
       └──────────┬─────────┘
                  │
                  │ SQL Queries
                  ▼
         ┌────────────────┐
         │   PostgreSQL   │
         │    Database    │
         │                │
         │   lms (DB)     │
         │  Port: 5432    │
         └────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│                         DOCKER NETWORK: app-network                      │
│                    All services communicate internally                   │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## Micro Frontend Isolation

```
┌────────────────────────────────────────────────────────────────────┐
│                      ADMIN PORTAL (Port 3000)                       │
│  ┌────────────────────────────────────────────────────────┐        │
│  │  Features:                                              │        │
│  │  • Student Groups Management                            │        │
│  │  • Class Schedule Management                            │        │
│  │  • Course Management                                    │        │
│  │  • Teacher Assignments                                  │        │
│  │  • System Configuration                                 │        │
│  └────────────────────────────────────────────────────────┘        │
│                                                                      │
│  Technology Stack:                                                  │
│  • Next.js 15 + TypeScript                                         │
│  • Tailwind CSS + shadcn/ui                                        │
│  • Bun Package Manager                                             │
└─────────────────────────────┬──────────────────────────────────────┘
                              │
                              │ API Calls
                              ▼
┌────────────────────────────────────────────────────────────────────┐
│                     TEACHER PORTAL (Port 3001)                      │
│  ┌────────────────────────────────────────────────────────┐        │
│  │  Features:                                              │        │
│  │  • Attendance Marking                                   │        │
│  │  • Grade Entry                                          │        │
│  │  • Course Content Management                            │        │
│  │  • Student Lists                                        │        │
│  │  • Schedule Viewing                                     │        │
│  └────────────────────────────────────────────────────────┘        │
│                                                                      │
│  Technology Stack:                                                  │
│  • Next.js 15 + TypeScript                                         │
│  • Tailwind CSS + shadcn/ui                                        │
│  • Bun Package Manager                                             │
└─────────────────────────────┬──────────────────────────────────────┘
                              │
                              │ API Calls
                              ▼
┌────────────────────────────────────────────────────────────────────┐
│                     STUDENT PORTAL (Port 3002)                      │
│  ┌────────────────────────────────────────────────────────┐        │
│  │  Features:                                              │        │
│  │  • Attendance Viewing                                   │        │
│  │  • Grade Viewing                                        │        │
│  │  • Course Enrollment                                    │        │
│  │  • Schedule Viewing                                     │        │
│  │  • Profile Management                                   │        │
│  └────────────────────────────────────────────────────────┘        │
│                                                                      │
│  Technology Stack:                                                  │
│  • Next.js 15 + TypeScript                                         │
│  • Tailwind CSS + shadcn/ui                                        │
│  • Bun Package Manager                                             │
└─────────────────────────────┬──────────────────────────────────────┘
                              │
                              ▼
                    ┌──────────────────┐
                    │  Unified Backend │
                    │   Django + API   │
                    └──────────────────┘
```

---

## Data Flow Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                    USER AUTHENTICATION FLOW                      │
└─────────────────────────────────────────────────────────────────┘

1. User Login Request
   Frontend → Nginx → Backend-Django → Database
   
2. Token Generation
   Database ← Backend-Django ← Nginx ← Frontend
   
3. Authenticated Requests
   Frontend (+Token) → Nginx → Backend APIs → Database

┌─────────────────────────────────────────────────────────────────┐
│                    DATA OPERATION FLOW                           │
└─────────────────────────────────────────────────────────────────┘

1. User Action (e.g., Mark Attendance)
   ┌──────────────┐
   │ User Clicks  │
   │   Button     │
   └───────┬──────┘
           │
           ▼
   ┌──────────────┐
   │  Frontend    │  React State Update
   │  Component   │  Form Validation
   └───────┬──────┘
           │
           ▼ HTTP POST /api/attendance
   ┌──────────────┐
   │    Nginx     │  Route to backend
   └───────┬──────┘
           │
           ▼
   ┌──────────────┐
   │   Backend    │  Validate Token
   │   (Django)   │  Business Logic
   └───────┬──────┘
           │
           ▼ SQL INSERT
   ┌──────────────┐
   │  PostgreSQL  │  Store Data
   │   Database   │  Return Result
   └───────┬──────┘
           │
           ▼ Response
   Frontend ← Nginx ← Backend
   
2. UI Update
   ┌──────────────┐
   │  Frontend    │  Display Success
   │  Component   │  Update UI
   └──────────────┘
```

---

## Service Dependencies

```
┌─────────────────────────────────────────────────────────────────┐
│                    SERVICE STARTUP ORDER                         │
└─────────────────────────────────────────────────────────────────┘

1. Database (db)
   ↓ depends_on (health check)
   
2. Backend Services
   ├── backend-django
   └── backend-fastapi
       ↓ depends_on
       
3. Frontend Services
   ├── frontend-admin
   ├── frontend-teacher
   └── frontend-student
       ↓ depends_on
       
4. Gateway
   └── nginx

┌─────────────────────────────────────────────────────────────────┐
│                    FAILURE ISOLATION                             │
└─────────────────────────────────────────────────────────────────┘

Scenario 1: Admin Frontend Crashes
  ✅ Teacher Portal: Still Working
  ✅ Student Portal: Still Working
  ✅ Backend APIs: Still Working
  ❌ Admin Portal: Down

Scenario 2: Database Crashes
  ❌ All Services: Cannot access data
  ⚠️  Frontends: Show cached data
  ⚠️  Backends: Return error responses

Scenario 3: Nginx Crashes
  ✅ Services: Still running internally
  ❌ External Access: Not available
  💡 Solution: Direct port access possible
```

---

## Volume and Data Persistence

```
┌─────────────────────────────────────────────────────────────────┐
│                    DOCKER VOLUMES                                │
└─────────────────────────────────────────────────────────────────┘

📦 db-data
   └── /var/lib/postgresql/data
       ├── Tables
       ├── Indexes
       └── Database Files
       Purpose: Persist database data across container restarts

📦 backend-static
   └── /app/staticfiles
       ├── CSS Files
       ├── JavaScript
       └── Images
       Purpose: Django static files (CSS, JS, images)

📦 backend-media
   └── /app/media
       ├── User Uploads
       ├── Profile Pictures
       └── Documents
       Purpose: User uploaded files

📦 backend-uploads
   └── /app/uploads
       ├── Course Materials
       ├── Assignments
       └── Temporary Files
       Purpose: FastAPI file uploads

┌─────────────────────────────────────────────────────────────────┐
│                    BACKUP STRATEGY                               │
└─────────────────────────────────────────────────────────────────┘

Daily:
  • Database backup (pg_dump)
  • Media files backup

Weekly:
  • Full volume backup
  • Configuration backup

Monthly:
  • Archive old backups
  • Disaster recovery test
```

---

## Network Communication

```
┌─────────────────────────────────────────────────────────────────┐
│               DOCKER NETWORK: app-network (bridge)               │
└─────────────────────────────────────────────────────────────────┘

Internal Service Communication:
┌──────────────────┐
│ frontend-admin   │──→ http://backend-django:8001
│ (container)      │──→ http://backend-fastapi:8000
└──────────────────┘

┌──────────────────┐
│ backend-django   │──→ postgresql://db:5432/lms
│ (container)      │
└──────────────────┘

┌──────────────────┐
│ backend-fastapi  │──→ postgresql://db:5432/lms
│ (container)      │
└──────────────────┘

External Access (from host):
http://localhost:3000  → frontend-admin
http://localhost:3001  → frontend-teacher
http://localhost:3002  → frontend-student
http://localhost:8000  → backend-fastapi
http://localhost:8001  → backend-django
http://localhost:5432  → db
http://localhost:80    → nginx

Production (via domain):
https://yourdomain.com/          → nginx → frontend-admin
https://yourdomain.com/teacher/  → nginx → frontend-teacher
https://yourdomain.com/student/  → nginx → frontend-student
https://yourdomain.com/api/      → nginx → backends
```

---

## Scaling Strategy

```
┌─────────────────────────────────────────────────────────────────┐
│                    HORIZONTAL SCALING                            │
└─────────────────────────────────────────────────────────────────┘

Low Traffic (Development):
  1x db
  1x backend-django
  1x backend-fastapi
  1x frontend-admin
  1x frontend-teacher
  1x frontend-student
  1x nginx

Medium Traffic (Production):
  1x db (with replication)
  2x backend-django
  2x backend-fastapi
  2x frontend-admin
  2x frontend-teacher
  3x frontend-student  ← Most users
  2x nginx (load balanced)

High Traffic (Enterprise):
  1x db-primary + 2x db-replica
  4x backend-django
  4x backend-fastapi
  3x frontend-admin
  5x frontend-teacher
  10x frontend-student  ← Most users
  3x nginx (load balanced)
  + CDN for static assets
  + Redis for caching
  + Message Queue (RabbitMQ/Redis)
```

---

## Deployment Pipeline

```
┌─────────────────────────────────────────────────────────────────┐
│                    CI/CD PIPELINE                                │
└─────────────────────────────────────────────────────────────────┘

1. Code Push to Git
   │
   ▼
2. Run Tests
   ├── Frontend Tests (Jest, Cypress)
   ├── Backend Tests (pytest, unittest)
   └── Integration Tests
   │
   ▼
3. Build Docker Images
   ├── Build frontend-admin
   ├── Build frontend-teacher
   ├── Build frontend-student
   ├── Build backend-django
   └── Build backend-fastapi
   │
   ▼
4. Push to Registry
   └── Docker Hub / AWS ECR / Azure ACR
   │
   ▼
5. Deploy to Environment
   ├── Development → Auto deploy
   ├── Staging → Manual approval
   └── Production → Manual approval + rollback plan
   │
   ▼
6. Health Checks
   ├── Service availability
   ├── Database connectivity
   └── Frontend accessibility
   │
   ▼
7. Monitoring
   ├── Logs (ELK Stack)
   ├── Metrics (Prometheus)
   └── Alerts (Grafana)
```

---

## Security Layers

```
┌─────────────────────────────────────────────────────────────────┐
│                    SECURITY ARCHITECTURE                         │
└─────────────────────────────────────────────────────────────────┘

Layer 1: Network Security
  • Firewall rules (only 80/443 exposed)
  • DDoS protection
  • Rate limiting

Layer 2: Gateway Security (Nginx)
  • SSL/TLS termination
  • Request validation
  • Header security
  • CORS policies

Layer 3: Application Security
  • JWT token authentication
  • Role-based access control
  • Input validation
  • SQL injection prevention
  • XSS protection

Layer 4: Data Security
  • Encrypted connections
  • Password hashing
  • Data encryption at rest
  • Backup encryption

Layer 5: Container Security
  • Non-root users
  • Read-only filesystems
  • Security scanning
  • Resource limits
```

---

## Monitoring Dashboard Layout

```
┌────────────────────────────────────────────────────────────────┐
│                    GRAFANA DASHBOARD                            │
└────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────┐
│  System Overview                                              │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐    │
│  │   CPU    │  │  Memory  │  │   Disk   │  │ Network  │    │
│  │   45%    │  │   60%    │  │   30%    │  │  20MB/s  │    │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘    │
└──────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────┐
│  Service Status                                               │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  ● db              ● backend-django                   │   │
│  │  ● backend-fastapi ● frontend-admin                   │   │
│  │  ● frontend-teacher ● frontend-student                │   │
│  │  ● nginx                                               │   │
│  └──────────────────────────────────────────────────────┘   │
└──────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────┐
│  Request Metrics                                              │
│  ┌────────────────────────────────────────────────────┐     │
│  │  Requests/sec:  125                                 │     │
│  │  Avg Response:  120ms                               │     │
│  │  Error Rate:    0.1%                                │     │
│  │  Active Users:  1,250                               │     │
│  └────────────────────────────────────────────────────┘     │
└──────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────┐
│  Database Metrics                                             │
│  ┌────────────────────────────────────────────────────┐     │
│  │  Connections:    25/100                             │     │
│  │  Query Time:     45ms avg                           │     │
│  │  Cache Hit:      85%                                │     │
│  │  Size:           2.5GB                              │     │
│  └────────────────────────────────────────────────────┘     │
└──────────────────────────────────────────────────────────────┘
```

This visual guide provides a comprehensive overview of the entire system architecture!
